#include <stdio.h>
/**
*main -Entry point
*
*Description: this program print the alphbet inverse
*
*Return: Always 0 (success)
*/
int main(void)
{
char lc;
for (lc = 'z' ; lc <= 'a'; lc++)
{
putchar(lc);

}
putchar('\n');

return (0);
}

#include <stdio.h>
/**
*main - Entry point
*
*description: the program will pritn numbers and ,  and space
*
*Return: always 0 (success)
*
*/
int main(void)
{
for (int i = 0 ; i < 10; i++)
{
putchar(i + '0')
if (i < 9)
{
putchar(',');
putchar(' ');
}
}
putchar('\n');
return (0);
}
